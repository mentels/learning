ExUnit.start(trace: true)

defmodule TuplesTest do
  @moduledoc """
  Tests for deriving with options
  """
  use ExUnit.Case

 test "test that opts are passed to __derive__/3" do
    assert WithOpts.
 end

end
